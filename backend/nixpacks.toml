[phases.setup]
nixPkgs = ["python312", "gcc", "libGL", "glib"]

[phases.install]
cmds = ["pip install uv", "uv sync --frozen --no-install-project"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"

[variables]
PYTHONDONTWRITEBYTECODE = "1"
PYTHONUNBUFFERED = "1"
